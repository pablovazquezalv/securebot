import { FilterRequestPipe } from './filter-request.pipe';

describe('FilterRequestPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRequestPipe();
    expect(pipe).toBeTruthy();
  });
});
