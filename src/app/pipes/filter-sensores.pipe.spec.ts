import { FilterSensoresPipe } from './filter-sensores.pipe';

describe('FilterSensoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSensoresPipe();
    expect(pipe).toBeTruthy();
  });
});
