import { FilterCompanyPipe } from './filter-company.pipe';

describe('FilterCompanyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompanyPipe();
    expect(pipe).toBeTruthy();
  });
});
