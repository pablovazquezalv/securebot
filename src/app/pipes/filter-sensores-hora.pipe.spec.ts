import { FilterSensoresHoraPipe } from './filter-sensores-hora.pipe';

describe('FilterSensoresHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSensoresHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
